#Starting with R assignment

#Question 1: Use R to figure out how many elements in the vector below are greater than 2.
rooms <- c(1, 5, 2, 3, 1, NA, 3, 1, 3, 2, NA, 1, 8, 3, 1, 4, NA, 1, 3, 1, 2, 1, 7, 1, NA, 4, 3 ,1 ,7 ,2 ,1 ,NA ,1, 1, 3)

#Then we define a new vector filtering out the missing data
vectors_greater_than_2 <- rooms[rooms>2 & !is.na(rooms)]

vectors_greater_than_2

# Question 2: Which function tells you the type of data the 'rooms' vector above contains?

class(rooms)
#In the console, the result of the running of this command is "numeric" 

#Question 3:
median_rooms <- median

median_rooms_without_NA <- median(rooms, na.rm=TRUE)
#In the environment the result of this command shows and tells us that the median of the vector "rooms" is 2.




